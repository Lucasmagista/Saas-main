import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ASSISTENCIAProfile = () => (
  <div className="p-8">
    <Card>
      <CardHeader>
        <CardTitle>Perfil de Assistência</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Área exclusiva para ASSISTENCIA. Gerencie atendimentos e suporte.</p>
      </CardContent>
    </Card>
  </div>
);

export default ASSISTENCIAProfile;
