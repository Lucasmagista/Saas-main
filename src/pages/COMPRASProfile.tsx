import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const COMPRASProfile = () => (
  <div className="p-8">
    <Card>
      <CardHeader>
        <CardTitle>Perfil de Compras</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Área exclusiva para COMPRAS. Gerencie fornecedores, pedidos e logística.</p>
      </CardContent>
    </Card>
  </div>
);

export default COMPRASProfile;
