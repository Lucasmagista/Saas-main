/*
 * Este arquivo foi mantido por compatibilidade, mas sua funcionalidade foi
 * consolidada no servidor unificado em `backend/index.cjs`. Caso queira
 * iniciar a aplicação, execute `node backend/index.cjs`. Este módulo
 * simplesmente emite um aviso para desenvolvedores e encerra a execução.
 */
require('dotenv').config();
console.warn('Aviso: src/server/index.js está obsoleto. Utilize backend/index.cjs para iniciar o servidor.');
